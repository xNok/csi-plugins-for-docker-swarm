version: '3'

services:
  minio:
    image: docker.io/bitnami/minio:2025
    ports:
      - '9000:9000'
      - '9001:9001'
    networks:
      - minionetwork
    volumes:
      - 'minio_data:/data'
    environment:
      - MINIO_ROOT_USER=test
      - MINIO_ROOT_PASSWORD=test
      - MINIO_DEFAULT_BUCKETS=your_buctest ket_name

networks:
  minionetwork:
    driver: bridge

volumes:
  minio_data:
    driver: local